{
    "scripts": {
        "heroku-prebuild": "cd theme/static_src && export NPM_CONFIG_PRODUCTION=false; export NODE_ENV=; NPM_CONFIG_PRODUCTION=false NODE_ENV=development npm install && npm run build",
        "heroku-postbuild": "export NPM_CONFIG_PRODUCTION=true; export NODE_ENV=production;",
        "start": "cd theme/static_src && npm start"
    }
}